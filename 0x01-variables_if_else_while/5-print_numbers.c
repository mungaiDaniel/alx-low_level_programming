#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int alphabets;

	for (alphabets = 0; alphabets < 10; alphabets++)
	{
		printf("%d", alphabets);
	}
	printf("\n");

	return (0);
}

