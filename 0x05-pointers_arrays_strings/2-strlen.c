#include "main.h"
#include <string.h>

/**
 * _strlen - return the length of a string
 * @s: parametares for the function
 * Return: lent
 */
int _strlen(char *s)
{
	int lent;

	lent = strlen(s);

	return (lent);
}
