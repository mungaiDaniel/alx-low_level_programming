#include "main.h"
#include <string.h>

/**
 * print_rev - print a string in reverse
 * @s: parametares for the function
 * Return: nothing
 */
void print_rev(char *s)
{
	int lent;

	lent = strlen(s);

	while (len--)
		putchar(*(s + len));
	putchar('\n');
}
