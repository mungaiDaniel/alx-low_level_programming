#include "main.h"

/**
 * mul - multiplies two integers
 * @a: integer one to multiply
 * @b: integer two to multiply
 * Return: the results
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
